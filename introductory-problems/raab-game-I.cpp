#include <bits/stdc++.h>

using namespace std;

int main(){

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    vector<int> n;

    int t;
    cin >> t;
    
    for(int i = 0; i < n.size(); i ++){
        cin >> n[i];
    }

    return 0;

}